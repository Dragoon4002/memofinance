{
  "name": "memofinance",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @memofinance/mcp dev",
    "setup": "pnpm install && npx node-gyp rebuild --directory node_modules/.pnpm/better-sqlite3@11.10.0/node_modules/better-sqlite3 2>/dev/null || true && pnpm build",
    "start": "node packages/mcp/dist/index.js"
  }
}
